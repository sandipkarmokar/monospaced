export const Javascript = `
const greeter = (name) => {
  console.log(name);
};

greeter("alex");
const greeter = (name) => {
  console.log(name);
};

greeter("alex");
const greeter = (name) => {
  console.log(name);
};

greeter("alex");
`;
